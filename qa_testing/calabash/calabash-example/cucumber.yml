#YAML Template
#cucumber.yml

#Android
<% common_path = " -r features/support -r features/step_definitions" %>
<% android_common_path = " -r features/android/support -r features/android/helpers -r features/android/screens" %>

android: RESET_BETWEEN_SCENARIOS=1 PLATFORM=android <%= common_path %> <%= android_common_path %> -v